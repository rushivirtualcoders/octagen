# 3D models

Drop optimized GLB assets here and wire them into `src/components/3d/`.

Expected files:

- `sports-car.glb` — fictional hypercar for the hero (replaces the cinematic image plate in `Hero.tsx`)
- `engine.glb` — detailed engine block (replaces the procedural engine in `EngineScene.tsx`)

Guidelines: Draco/Meshopt-compress, keep textures ≤ 2K KTX2, target < 3 MB per model.
