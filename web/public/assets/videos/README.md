# Videos

- `hero-background.mp4` — LIQUI MOLY 2019 commercial (muted, 15s loop) used as the homepage hero atmosphere.

Drop additional loops here if needed:

- `hero-mobile.mp4` — lighter mobile hero loop
- `racing.mp4` — night racing loop for the Racing section

All videos must be muted, looped, `playsInline`, with a poster image in `../images/`.
