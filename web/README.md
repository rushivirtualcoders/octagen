# Octagen web

React + Vite landing page. CMS/admin is paused.

```bash
cd web
npm install
npm run dev
```

Site: http://localhost:3000

Visual reference: to be added (3D car cinematic design). Do not use the old Unsplash car-slideshow HTML.
